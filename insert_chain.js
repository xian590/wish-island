const fs = require('fs');
const html = fs.readFileSync('C:/Users/Administrator/Documents/kimi/workspace/farm_game.html', 'utf8');

// 1. Modify onNewDay
const onNewDayOld = '    // 随机事件（15%概率触发）\r\n    if (!game.currentEvent && Math.random() < 0.15) {\r\n        triggerRandomEvent();\r\n    }';
const onNewDayNew = '    // NPC主动事件链\r\n    checkNpcChainEvents();\r\n    \r\n    // 随机事件（15%概率触发）\r\n    if (!game.currentEvent && Math.random() < 0.15) {\r\n        triggerRandomEvent();\r\n    }';

let newHtml = html.replace(onNewDayOld, onNewDayNew);
if (newHtml === html) {
    console.error('onNewDay replace failed');
    process.exit(1);
}

// 2. Add NPC chain functions after handleEventChoice
const insertOld = '    // 关闭弹窗\r\n    closeModal();\r\n    \r\n    saveGame();\r\n    updateUI();\r\n}\r\n\r\nfunction growCrops() {';

const insertNew = '    // 关闭弹窗\r\n    closeModal();\r\n    \r\n    saveGame();\r\n    updateUI();\r\n}\r\n\r\n// ==================== NPC主动事件链 ====================\r\nfunction checkNpcChainEvents() {\r\n    if (!game.npcChainCompleted) game.npcChainCompleted = {};\r\n    if (game.currentEvent) return;\r\n    \r\n    for (const eventId in NPC_CHAIN_EVENTS) {\r\n        const event = NPC_CHAIN_EVENTS[eventId];\r\n        \r\n        // 检查是否已完成\r\n        if (game.npcChainCompleted[eventId]) continue;\r\n        \r\n        // 检查前置事件\r\n        if (event.requireCompleted && !game.npcChainCompleted[event.requireCompleted]) continue;\r\n        \r\n        // 检查天数\r\n        if (game.day < event.requireDay) continue;\r\n        \r\n        // 检查好感度\r\n        const friendship = game.npcs[event.npc] || 0;\r\n        if (friendship < event.requireFriendship) continue;\r\n        \r\n        // 触发事件\r\n        game.currentEvent = eventId;\r\n        game.currentEventIsNpcChain = true;\r\n        showNpcChainModal(event);\r\n        return; // 一次只触发一个\r\n    }\r\n}\r\n\r\nfunction showNpcChainModal(event) {\r\n    let html = \'<div style="text-align: center; margin-bottom: 20px;">\';\r\n    html += \'<div style="font-size: 60px; margin-bottom: 10px;">\' + event.emoji + \'</div>\';\r\n    html += \'<div style="font-size: 20px; font-weight: bold; margin-bottom: 15px; color: #e67e22;">\' + event.name + \'</div>\';\r\n    html += \'<div style="font-size: 14px; line-height: 1.8; color: #666; margin-bottom: 20px;">\' + event.description + \'</div>\';\r\n    html += \'</div>\';\r\n    \r\n    html += \'<div style="display: flex; flex-direction: column; gap: 10px;">\';\r\n    event.options.forEach((opt, idx) => {\r\n        html += \'<button class="modal-btn" onclick="handleNpcChainChoice(\' + idx + \')">\' + opt.text + \'</button>\';\r\n    });\r\n    html += \'</div>\';\r\n    \r\n    document.getElementById(\'modal-title\').textContent = \'NPC事件\';\r\n    document.getElementById(\'modal-content\').innerHTML = html;\r\n    document.getElementById(\'modal-actions\').style.display = \'none\';\r\n    document.getElementById(\'modal\').style.display = \'flex\';\r\n}\r\n\r\nfunction handleNpcChainChoice(optionIdx) {\r\n    const eventId = game.currentEvent;\r\n    const event = NPC_CHAIN_EVENTS[eventId];\r\n    const option = event.options[optionIdx];\r\n    const effect = option.effect;\r\n    \r\n    // 应用效果\r\n    if (effect.stamina) {\r\n        game.stamina = Math.max(0, Math.min(game.maxStamina, game.stamina + effect.stamina));\r\n    }\r\n    if (effect.money) {\r\n        game.money += effect.money;\r\n    }\r\n    if (effect.reputation) {\r\n        game.reputation += effect.reputation;\r\n    }\r\n    if (effect.npcFriendship) {\r\n        for (const npcKey in effect.npcFriendship) {\r\n            game.npcs[npcKey] = (game.npcs[npcKey] || 0) + effect.npcFriendship[npcKey];\r\n        }\r\n    }\r\n    if (effect.yieldLoss) {\r\n        game.fields.forEach(field => {\r\n            if (field.crop && field.stage !== \'mature\' && field.stage !== \'idle\') {\r\n                field.yieldLoss = (field.yieldLoss || 0) + effect.yieldLoss;\r\n            }\r\n        });\r\n    }\r\n    if (effect.yieldBonus) {\r\n        game.fields.forEach(field => {\r\n            if (field.crop && field.stage !== \'mature\' && field.stage !== \'idle\') {\r\n                field.yieldBonus = (field.yieldBonus || 0) + effect.yieldBonus;\r\n            }\r\n        });\r\n    }\r\n    if (effect.nextSeasonBonus) {\r\n        game.nextSeasonBonus = (game.nextSeasonBonus || 0) + effect.nextSeasonBonus;\r\n    }\r\n    \r\n    // 记录完成\r\n    if (!game.npcChainCompleted) game.npcChainCompleted = {};\r\n    game.npcChainCompleted[eventId] = true;\r\n    \r\n    // 记录事件\r\n    game.eventHistory.push({\r\n        day: game.day,\r\n        season: game.season,\r\n        eventId: eventId,\r\n        choice: optionIdx,\r\n        result: option.result,\r\n        isNpcChain: true\r\n    });\r\n    \r\n    addLog(\'💬 \' + event.name + \'：\' + option.result, \'good\');\r\n    \r\n    // 清除当前事件\r\n    game.currentEvent = null;\r\n    game.currentEventIsNpcChain = false;\r\n    \r\n    // 防止浮点数累积\r\n    roundGameState();\r\n    \r\n    // 关闭弹窗\r\n    closeModal();\r\n    \r\n    saveGame();\r\n    updateUI();\r\n}\r\n\r\nfunction growCrops() {';

newHtml = newHtml.replace(insertOld, insertNew);
if (newHtml === html) {
    console.error('handleEventChoice replace failed');
    process.exit(1);
}

fs.writeFileSync('C:/Users/Administrator/Documents/kimi/workspace/farm_game.html', newHtml);
console.log('NPC chain events logic inserted');
